using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class realGenericButtonListner : MonoBehaviour {
    public int buttonScreeen;
	// Use this for initialization
	void Start () {
        buttonScreeen = 0;

    }
	//9-17-20 this script is just a standardized public var locator. Build a script to grab the variable
	// Update is called once per frame
	void Update () {
		
	}
}
