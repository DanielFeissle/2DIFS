using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class title_menu_control : MonoBehaviour
{
    //12-17-2024
    //this controls the UI elements after 15 seconds of inactivity. 
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
