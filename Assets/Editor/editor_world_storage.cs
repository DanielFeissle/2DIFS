using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class editor_world_storage : MonoBehaviour
{
    public int WindCross = 1;
    int CloudLevel = 1;
    int Weather = 1;
    int FPS = 1;
    float RADBCK = 1;
    float RAD = 1;
    int spacing = 2;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
